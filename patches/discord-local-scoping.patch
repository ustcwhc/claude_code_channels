--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/server.ts.orig
+++ .claude/plugins/cache/claude-plugins-official/discord/0.0.1/server.ts
@@ -27,14 +27,49 @@
 import { randomBytes } from 'crypto'
 import { readFileSync, writeFileSync, mkdirSync, readdirSync, rmSync, statSync, renameSync, realpathSync, chmodSync } from 'fs'
 import { homedir } from 'os'
-import { join, sep } from 'path'
+import { join, sep, dirname } from 'path'
 
 const STATE_DIR = process.env.DISCORD_STATE_DIR ?? join(homedir(), '.claude', 'channels', 'discord')
 const ACCESS_FILE = join(STATE_DIR, 'access.json')
 const APPROVED_DIR = join(STATE_DIR, 'approved')
 const ENV_FILE = join(STATE_DIR, '.env')
 
+// discord-local-scoping patch applied
+// Resolve active access.json: project-local overrides global when present.
+// DISCORD_PROJECT_DIR is injected by Claude Code via .mcp.json env block (${CLAUDE_PROJECT_DIR}).
+const PROJECT_DIR = process.env.DISCORD_PROJECT_DIR
 
+function resolveAccessFile(): { path: string; scope: 'local' | 'global' } {
+  if (PROJECT_DIR) {
+    // Security: ensure resolved path is actually inside the stated project dir.
+    const candidate = join(PROJECT_DIR, '.claude', 'channels', 'discord', 'access.json')
+    if (!candidate.startsWith(PROJECT_DIR + '/') && candidate !== PROJECT_DIR) {
+      // Path traversal attempt — fall back silently.
+      process.stderr.write(`discord channel: DISCORD_PROJECT_DIR resolved outside project boundary, falling back to global\n`)
+      return { path: ACCESS_FILE, scope: 'global' }
+    }
+    try {
+      statSync(candidate)  // throws ENOENT if absent
+      return { path: candidate, scope: 'local' }
+    } catch (e) {
+      if ((e as NodeJS.ErrnoException).code !== 'ENOENT') {
+        process.stderr.write(`discord channel: unexpected error stat-ing local access.json: ${e}\n`)
+      }
+      // ENOENT is normal — fall through to global
+    }
+  } else {
+    process.stderr.write(`discord channel: DISCORD_PROJECT_DIR not set — project scoping unavailable, using global config\n`)
+  }
+  return { path: ACCESS_FILE, scope: 'global' }
+}
+
+const { path: ACTIVE_ACCESS_FILE, scope: ACTIVE_SCOPE } = resolveAccessFile()
+if (ACTIVE_SCOPE === 'local') {
+  process.stderr.write(`discord channel: using local config ${ACTIVE_ACCESS_FILE}\n`)
+} else {
+  process.stderr.write(`discord channel: using global config ${ACTIVE_ACCESS_FILE}\n`)
+}
+
 // Load ~/.claude/channels/discord/.env into process.env. Real env wins.
 // Plugin-spawned servers don't get an env block — this is where the token lives.
 try {
@@ -140,7 +175,7 @@
 
 function readAccessFile(): Access {
   try {
-    const raw = readFileSync(ACCESS_FILE, 'utf8')
+    const raw = readFileSync(ACTIVE_ACCESS_FILE, 'utf8')
     const parsed = JSON.parse(raw) as Partial<Access>
     return {
       dmPolicy: parsed.dmPolicy ?? 'pairing',
@@ -155,7 +190,13 @@
     }
   } catch (err) {
     if ((err as NodeJS.ErrnoException).code === 'ENOENT') return defaultAccess()
-    try { renameSync(ACCESS_FILE, `${ACCESS_FILE}.corrupt-${Date.now()}`) } catch {}
+    // D-08: local access.json corrupt → error and stop (force user to fix)
+    // Global access.json corrupt → move aside and start fresh (existing behavior)
+    if (ACTIVE_SCOPE === 'local') {
+      process.stderr.write(`discord channel: local access.json is corrupt — fix or delete ${ACTIVE_ACCESS_FILE}\n`)
+      process.exit(1)
+    }
+    try { renameSync(ACTIVE_ACCESS_FILE, `${ACTIVE_ACCESS_FILE}.corrupt-${Date.now()}`) } catch {}
     process.stderr.write(`discord: access.json is corrupt, moved aside. Starting fresh.\n`)
     return defaultAccess()
   }
@@ -184,10 +225,10 @@
 
 function saveAccess(a: Access): void {
   if (STATIC) return
-  mkdirSync(STATE_DIR, { recursive: true, mode: 0o700 })
-  const tmp = ACCESS_FILE + '.tmp'
+  mkdirSync(dirname(ACTIVE_ACCESS_FILE), { recursive: true, mode: 0o700 })
+  const tmp = ACTIVE_ACCESS_FILE + '.tmp'
   writeFileSync(tmp, JSON.stringify(a, null, 2) + '\n', { mode: 0o600 })
-  renameSync(tmp, ACCESS_FILE)
+  renameSync(tmp, ACTIVE_ACCESS_FILE)
 }
 
 function pruneExpired(a: Access): boolean {
--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/.mcp.json.orig
+++ .claude/plugins/cache/claude-plugins-official/discord/0.0.1/.mcp.json
@@ -2,7 +2,10 @@
   "mcpServers": {
     "discord": {
       "command": "bun",
-      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--shell=bun", "--silent", "start"]
+      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--shell=bun", "--silent", "start"],
+      "env": {
+        "DISCORD_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
+      }
     }
   }
 }
--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig
+++ .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md
@@ -7,6 +7,7 @@
   - Write
   - Bash(ls *)
   - Bash(mkdir *)
+  - Bash(printenv *)
 ---
 
 # /discord:access — Discord Channel Access Management
@@ -19,7 +20,8 @@
 downstream of untrusted input.
 
 Manages access control for the Discord channel. All state lives in
-`~/.claude/channels/discord/access.json`.
+`~/.claude/channels/discord/access.json` (global) or
+`<PROJECT_DIR>/.claude/channels/discord/access.json` (local, when active).
 You never talk to Discord — you just edit JSON; the channel server re-reads it.
 
 Arguments passed: `$ARGUMENTS`
@@ -28,7 +30,7 @@
 
 ## State shape
 
-`~/.claude/channels/discord/access.json`:
+`~/.claude/channels/discord/access.json` (or local equivalent):
 
 ```json
 {
@@ -51,17 +53,36 @@
 
 ---
 
+## Scope resolution
+
+Before any read or write, resolve which access.json to use:
+
+1. Run `Bash(printenv DISCORD_PROJECT_DIR)` — if the output is non-empty, PROJECT_DIR is set.
+2. If PROJECT_DIR is set: check if `<PROJECT_DIR>/.claude/channels/discord/access.json` exists by running `Bash(ls <PROJECT_DIR>/.claude/channels/discord/access.json 2>/dev/null)`. If the file is listed (exit 0): SCOPE=local, ACTIVE_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`.
+3. If PROJECT_DIR is not set, or the local file is absent: SCOPE=global, ACTIVE_PATH=`~/.claude/channels/discord/access.json`.
+
+Call this at the start of every operation that reads or writes access.json. Exceptions:
+- `pair`, `deny`, `allow`, `remove`, `policy` always use ACTIVE_PATH=`~/.claude/channels/discord/access.json` (global only — DM/user-level operations are not project-scoped).
+- `set` uses ACTIVE_PATH (the active resolved file).
+
+---
+
 ## Dispatch on arguments
 
 Parse `$ARGUMENTS` (space-separated). If empty or unrecognized, show status.
 
 ### No args — status
 
-1. Read `~/.claude/channels/discord/access.json` (handle missing file with default).
-2. Show: dmPolicy, allowFrom count and list, pending count with codes + sender IDs + age, groups count.
+1. Run scope resolution (above) to get ACTIVE_PATH and SCOPE.
+2. Print banner: `Using: local (<ACTIVE_PATH>)` when SCOPE=local, or `Using: global (<ACTIVE_PATH>)` when SCOPE=global. Use the full absolute path from ACTIVE_PATH — not an abbreviated form.
+3. Read ACTIVE_PATH (handle missing file with default).
+4. Show: dmPolicy, allowFrom count and list, pending count with codes + sender IDs + age, groups count.
+5. Duplicate-group check: if SCOPE=local and groups exist in local config, also read the global config (`~/.claude/channels/discord/access.json`). For each channelId found in BOTH configs, print: `⚠ Channel <channelId> also exists in global config. Local config takes precedence when active.` This is informational only — do not block.
 
 ### `pair <code>`
 
+Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope.
+
 1. Read `~/.claude/channels/discord/access.json`.
 2. Look up `pending[<code>]`. If not found or `expiresAt < Date.now()`,
    tell the user and stop.
@@ -76,36 +97,62 @@
 
 ### `deny <code>`
 
+Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope.
+
 1. Read `~/.claude/channels/discord/access.json`, delete `pending[<code>]`, write back.
 2. Confirm.
 
 ### `allow <senderId>`
 
+Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope.
+
 1. Read `~/.claude/channels/discord/access.json` (create default if missing).
 2. Add `<senderId>` to `allowFrom` (dedupe).
 3. Write back.
 
 ### `remove <senderId>`
 
+Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope.
+
 1. Read `~/.claude/channels/discord/access.json`, filter `allowFrom` to exclude `<senderId>`, write.
 
 ### `policy <mode>`
 
+Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope.
+
 1. Validate `<mode>` is one of `pairing`, `allowlist`, `disabled`.
 2. Read `~/.claude/channels/discord/access.json` (create default if missing), set `dmPolicy`, write.
 
-### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`)
+### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`, `--local`, `--global`)
 
-1. Read `~/.claude/channels/discord/access.json` (create default if missing).
-2. Set `groups[<channelId>] = { requireMention: !hasFlag("--no-mention"), allowFrom: parsedAllowList }`.
-3. Write back (pretty-print, 2-space indent).
+1. Run scope resolution to get PROJECT_DIR and whether the local file exists.
+2. Check for `--local` or `--global` flag in $ARGUMENTS (flags bypass the scope prompt):
+   - If `--local` is present:
+     - If PROJECT_DIR is not set: error "Cannot use --local: DISCORD_PROJECT_DIR is not set. Set it via .mcp.json or run from a Claude Code session with a project directory." Stop.
+     - TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`. Create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
+   - If `--global` is present: TARGET_PATH=`~/.claude/channels/discord/access.json`.
+   - If neither flag: continue to step 3 (interactive prompt).
+3. If neither --local nor --global: If PROJECT_DIR is set:
+   a. Ask user: "Add channel `<channelId>` to your project-local config (`.claude/channels/discord/access.json`) or global config (`~/.claude/channels/discord/access.json`)? [local/global]" — use AskUserQuestion. Default suggestion: "local" (first option).
+   b. If user answers "local": TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`, create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
+   c. If user answers "global": TARGET_PATH=`~/.claude/channels/discord/access.json`.
+   If PROJECT_DIR is not set: warn "Note: DISCORD_PROJECT_DIR is not set — project-local scoping unavailable. Writing to global config." TARGET_PATH=`~/.claude/channels/discord/access.json`.
+4. Read TARGET_PATH (create default if missing).
+4a. Duplicate-group check: read the OTHER config file (the one not being written to). If `groups[<channelId>]` exists there too, warn: `⚠ Channel <channelId> also exists in [local/global] config. Local config takes precedence when active.` Continue — do not block.
+5. Set `groups[<channelId>] = { requireMention: !hasFlag("--no-mention"), allowFrom: parsedAllowList }`.
+6. Write to TARGET_PATH (pretty-print, 2-space indent).
 
 ### `group rm <channelId>`
 
-1. Read `~/.claude/channels/discord/access.json`.
-2. Delete `groups[<channelId>]`.
-3. Write back.
-4. Confirm.
+1. Run scope resolution: run `Bash(printenv DISCORD_PROJECT_DIR)` to get PROJECT_DIR. Determine both candidate paths:
+   - LOCAL_PATH = `<PROJECT_DIR>/.claude/channels/discord/access.json` (if DISCORD_PROJECT_DIR is set)
+   - GLOBAL_PATH = `~/.claude/channels/discord/access.json`
+2. Check if the group exists in local config (if PROJECT_DIR is set): Read LOCAL_PATH, check if `groups[<channelId>]` is present.
+3. Check if the group exists in global config: Read GLOBAL_PATH, check if `groups[<channelId>]` is present.
+4. If found in local: delete `groups[<channelId>]`, write LOCAL_PATH, confirm "Removed from local config."
+5. If found in global (and not already removed from local): delete `groups[<channelId>]`, write GLOBAL_PATH, confirm "Removed from global config."
+6. If found in neither: say "Channel `<channelId>` not found in local config or global config."
+7. If found in both (unlikely but possible): remove from both, confirm "Removed from both local and global config."
 
 ### `set <key> <value>`
 
@@ -117,7 +164,7 @@
 - `chunkMode`: `length` | `newline`
 - `mentionPatterns`: JSON array of regex strings
 
-Read `~/.claude/channels/discord/access.json`, set the key, write back, confirm.
+Run scope resolution to get ACTIVE_PATH. Read ACTIVE_PATH, set the key, write to ACTIVE_PATH, confirm.
 
 ---
 
