---
phase: 03-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md
  - patches/discord-local-scoping.patch
autonomous: true
requirements: [UX-01, UX-02, SKIL-06, SKIL-07]

must_haves:
  truths:
    - "Status output shows the full absolute path of the active config file (e.g. /Users/foo/project/.claude/channels/discord/access.json), not an abbreviated relative path"
    - "When the same channel group ID exists in both local and global config, status and group add both print a warning that is informational only"
    - "group add with --local flag skips the AskUserQuestion prompt and writes directly to the project-local file"
    - "group add with --local flag when DISCORD_PROJECT_DIR is unset errors with a clear message instead of silently falling back"
    - "group add with --global flag skips the AskUserQuestion prompt and writes directly to the global file"
    - "All changes survive plugin cache updates via the regenerated patch"
  artifacts:
    - path: "~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md"
      provides: "Updated skill with full path status, duplicate warning, and --local/--global flags"
      contains: "ACTIVE_PATH (full absolute path)"
    - path: "patches/discord-local-scoping.patch"
      provides: "Regenerated patch capturing the updated SKILL.md hunk alongside existing server.ts and .mcp.json hunks"
      contains: "SKILL.md.orig"
  key_links:
    - from: "SKILL.md status section"
      to: "ACTIVE_PATH (full path)"
      via: "scope resolution result printed verbatim"
      pattern: "ACTIVE_PATH"
    - from: "SKILL.md group add section"
      to: "--local/--global flag check"
      via: "flag detection before AskUserQuestion"
      pattern: "--local|--global"
    - from: "discord-local-scoping.patch"
      to: "SKILL.md"
      via: "third diff hunk"
      pattern: "SKILL.md.orig"
---

<objective>
Implement the four Phase 3 requirements in SKILL.md (full resolved path in status, duplicate-group warning, --local/--global flags on group add), then regenerate the SKILL.md hunk inside `discord-local-scoping.patch` so the changes survive plugin cache updates.

Purpose: Power users can bypass the interactive scope prompt; the status output gives enough detail to debug routing issues without guessing which file is active.
Output: Updated cached SKILL.md + updated patch file with the new SKILL.md hunk replacing the Phase 2 hunk.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ux-polish/03-CONTEXT.md

# Current state of the skill (Phase 2 output — this IS the file to modify):
@~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md

# Current patch (the SKILL.md hunk in this file will be replaced in Task 2):
@patches/discord-local-scoping.patch

# Apply script (no changes needed — already handles SKILL.md marker):
@scripts/apply-discord-patch.sh
</context>

<interfaces>
<!-- Key patterns established in Phase 2 SKILL.md (the file Task 1 modifies). -->
<!-- Executor must understand these to make targeted edits. -->

Current status section (lines ~76-79 in SKILL.md):
```
1. Run scope resolution (above) to get ACTIVE_PATH and SCOPE.
2. Print banner: `Using: local (.claude/channels/discord/access.json)` when SCOPE=local, or `Using: global (~/.claude/channels/discord/access.json)` when SCOPE=global.
3. Read ACTIVE_PATH (handle missing file with default).
4. Show: dmPolicy, allowFrom count and list, pending count with codes + sender IDs + age, groups count.
```

Current group add section (lines ~126-136 in SKILL.md):
```
### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`)

1. Run scope resolution to get PROJECT_DIR and whether the local file exists.
2. If PROJECT_DIR is set:
   a. Ask user: "Add channel `<channelId>` to your project-local config ..." — use AskUserQuestion. Default suggestion: "local".
   b. If user answers "local": TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`, create dir if needed.
   c. If user answers "global": TARGET_PATH=`~/.claude/channels/discord/access.json`.
3. If PROJECT_DIR is not set: warn ... TARGET_PATH=`~/.claude/channels/discord/access.json`.
4. Read TARGET_PATH (create default if missing).
5. Set `groups[<channelId>] = { requireMention: !hasFlag("--no-mention"), allowFrom: parsedAllowList }`.
6. Write to TARGET_PATH (pretty-print, 2-space indent).
```

Current group rm section already reads both files — reuse that dual-file pattern for duplicate detection.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md with full path, duplicate warning, and --local/--global flags</name>
  <files>~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md</files>
  <read_first>
    - ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md — read the full file first; make targeted edits to three sections only
  </read_first>
  <action>
Make three targeted edits to SKILL.md. Do NOT rewrite the whole file — edit only the specified sections.

**Edit 1 — Status section: full absolute path (per D-05, D-06, UX-01)**

Replace step 2 of the "No args — status" section:

OLD:
```
2. Print banner: `Using: local (.claude/channels/discord/access.json)` when SCOPE=local, or `Using: global (~/.claude/channels/discord/access.json)` when SCOPE=global.
```

NEW:
```
2. Print banner: `Using: local (<ACTIVE_PATH>)` when SCOPE=local, or `Using: global (<ACTIVE_PATH>)` when SCOPE=global. Use the full absolute path from ACTIVE_PATH — not an abbreviated form.
```

Also add a duplicate-check step after step 4 (per D-07, D-08, D-09, UX-02):

Add after the existing step 4 ("Show: dmPolicy..."):
```
5. Duplicate-group check: if SCOPE=local and groups exist in local config, also read the global config (`~/.claude/channels/discord/access.json`). For each channelId found in BOTH configs, print: `⚠ Channel <channelId> also exists in global config. Local config takes precedence when active.` This is informational only — do not block.
```

**Edit 2 — group add section: --local/--global flags (per D-01, D-02, D-03, D-04, SKIL-06, SKIL-07)**

Replace the section header and steps 1-3:

OLD:
```
### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`)

1. Run scope resolution to get PROJECT_DIR and whether the local file exists.
2. If PROJECT_DIR is set:
   a. Ask user: "Add channel `<channelId>` to your project-local config (`.claude/channels/discord/access.json`) or global config (`~/.claude/channels/discord/access.json`)? [local/global]" — use AskUserQuestion. Default suggestion: "local" (first option, consistent with D-02).
   b. If user answers "local": TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`, create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
   c. If user answers "global": TARGET_PATH=`~/.claude/channels/discord/access.json`.
3. If PROJECT_DIR is not set: warn "Note: DISCORD_PROJECT_DIR is not set — project-local scoping unavailable. Writing to global config." TARGET_PATH=`~/.claude/channels/discord/access.json`.
```

NEW:
```
### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`, `--local`, `--global`)

1. Run scope resolution to get PROJECT_DIR and whether the local file exists.
2. Check for `--local` or `--global` flag in $ARGUMENTS (flags bypass the scope prompt):
   - If `--local` is present:
     - If PROJECT_DIR is not set: error "Cannot use --local: DISCORD_PROJECT_DIR is not set. Set it via .mcp.json or run from a Claude Code session with a project directory." Stop.
     - TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`. Create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
   - If `--global` is present: TARGET_PATH=`~/.claude/channels/discord/access.json`.
   - If neither flag: continue to step 3 (interactive prompt).
3. If neither --local nor --global: If PROJECT_DIR is set:
   a. Ask user: "Add channel `<channelId>` to your project-local config (`.claude/channels/discord/access.json`) or global config (`~/.claude/channels/discord/access.json`)? [local/global]" — use AskUserQuestion. Default suggestion: "local" (first option).
   b. If user answers "local": TARGET_PATH=`<PROJECT_DIR>/.claude/channels/discord/access.json`, create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
   c. If user answers "global": TARGET_PATH=`~/.claude/channels/discord/access.json`.
   If PROJECT_DIR is not set: warn "Note: DISCORD_PROJECT_DIR is not set — project-local scoping unavailable. Writing to global config." TARGET_PATH=`~/.claude/channels/discord/access.json`.
```

Also add a duplicate-check step between the existing steps 4 and 5 (now renumbered) after reading TARGET_PATH (per D-07, D-08, D-09, UX-02):

After "4. Read TARGET_PATH (create default if missing)." add:
```
4a. Duplicate-group check: read the OTHER config file (the one not being written to). If `groups[<channelId>]` exists there too, warn: `⚠ Channel <channelId> also exists in [local/global] config. Local config takes precedence when active.` Continue — do not block.
```
  </action>
  <verify>
    <automated>grep -n "ACTIVE_PATH" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md | grep -i "full absolute" && grep -n "\-\-local" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md | head -5 && grep -n "\-\-global" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md | head -5 && grep -n "⚠" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "full absolute" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` matches (status banner uses full ACTIVE_PATH)
    - `grep "\-\-local" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 3 lines (section header, flag check, error case)
    - `grep "\-\-global" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 2 lines
    - `grep "⚠" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns 2 lines (one in status, one in group add)
    - `grep "Cannot use --local" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` matches (D-04 error case)
  </acceptance_criteria>
  <done>SKILL.md has: (1) status banner with full ACTIVE_PATH and duplicate-group warning step, (2) group add with --local/--global flag detection before the interactive prompt, (3) --local-without-PROJECT_DIR error case, (4) duplicate-check step in group add.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate SKILL.md patch hunk and update discord-local-scoping.patch</name>
  <files>patches/discord-local-scoping.patch</files>
  <read_first>
    - patches/discord-local-scoping.patch — read the full current patch; the SKILL.md hunk starts at the line containing "SKILL.md.orig" and runs to end of file; that hunk will be replaced
    - scripts/apply-discord-patch.sh — confirm SKILL_MARKER is still "## Scope resolution" (no changes needed to this file)
  </read_first>
  <action>
Generate a fresh diff for SKILL.md and replace the SKILL.md hunk in the patch file.

**Step 1 — Find the original SKILL.md baseline.**

The original (unpatched) version is captured as a `.orig` file if it exists:
```bash
ls ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig 2>/dev/null
```

If the `.orig` exists, use it as the baseline for diff. If it does not exist (because Phase 2 already applied the patch and no backup was created), reconstruct the baseline by applying the patch in reverse:
```bash
# In the project root:
patch -R -p0 --dry-run < patches/discord-local-scoping.patch
```
If the reverse dry-run succeeds on the SKILL.md hunk, produce the `.orig` by reversing just that hunk:
```bash
patch -R -p0 < patches/discord-local-scoping.patch
cp ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig
patch -p0 < patches/discord-local-scoping.patch
```
(This restores the current patched state after extracting the original.)

If reverse fails (already partially applied or file has drifted), use a different approach: The original SKILL.md content is known from the patch header in the current patch file — extract the `-` lines from the SKILL.md hunk and reconstruct.

**Step 2 — Produce the new diff hunk.**

Once the `.orig` baseline exists alongside the updated SKILL.md:
```bash
diff -u \
  ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig \
  ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md \
  | sed 's|^--- .*|--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig|' \
  | sed 's|^+++ .*|+++ .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md|'
```

Capture this output as NEW_SKILL_HUNK.

**Step 3 — Splice into patch file.**

Read `patches/discord-local-scoping.patch`. Find the start of the SKILL.md hunk: the line beginning with `--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig`. Everything from that line to the end of the file is the old SKILL.md hunk. Replace it with NEW_SKILL_HUNK.

Write the updated patch back to `patches/discord-local-scoping.patch`. Verify the file now has all three sections:
1. server.ts hunk (starts with `--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/server.ts.orig`)
2. .mcp.json hunk (starts with `--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/.mcp.json.orig`)
3. SKILL.md hunk (starts with `--- .claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md.orig`) — this is the newly generated one

**Step 4 — Verify the patch applies cleanly.**

Test that the new patch would apply successfully against the `.orig` baseline:
```bash
cd ~ && patch --dry-run -p0 < /Users/haocheng_mini/Documents/projects/claude_code_channels/patches/discord-local-scoping.patch
```
All three hunks must report success. If any fail, diagnose and fix the diff generation (likely a path mismatch or context line issue).
  </action>
  <verify>
    <automated>cd /Users/haocheng_mini/Documents/projects/claude_code_channels && grep -c "^---" patches/discord-local-scoping.patch | grep -q "3" && echo "3 hunks present" && grep "Cannot use --local" patches/discord-local-scoping.patch && grep "full absolute" patches/discord-local-scoping.patch</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^---" patches/discord-local-scoping.patch` equals 3 (server.ts, .mcp.json, SKILL.md hunks all present)
    - `grep "Cannot use --local" patches/discord-local-scoping.patch` matches (D-04 error is in the patch)
    - `grep "full absolute" patches/discord-local-scoping.patch` matches (UX-01 is in the patch)
    - `grep "⚠" patches/discord-local-scoping.patch` returns at least 1 match (UX-02 duplicate warning is in the patch)
    - `cd ~ && patch --dry-run -p0 < /Users/haocheng_mini/Documents/projects/claude_code_channels/patches/discord-local-scoping.patch` exits 0 (all hunks apply cleanly against originals)
  </acceptance_criteria>
  <done>The patch file contains all three hunks; the SKILL.md hunk includes all Phase 3 changes; a dry-run confirms the patch applies cleanly. Running apply-discord-patch.sh from a fresh plugin install would produce the fully Phase-3-enhanced SKILL.md.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Spot-check the updated SKILL.md status section — confirm the banner line references `ACTIVE_PATH` with "full absolute path" language.
2. Spot-check the group add section — confirm `--local` and `--global` appear before the AskUserQuestion block; confirm the error case for `--local` without PROJECT_DIR is present.
3. Spot-check the duplicate warning — confirm `⚠` appears in both the status section and the group add section.
4. Confirm `patches/discord-local-scoping.patch` has exactly 3 `---` header lines.
5. Confirm `patch --dry-run` from `$HOME` exits 0.
</verification>

<success_criteria>
Phase 3 is complete when:
- `/discord:access` (status) would show the full absolute path of the active config, not an abbreviated form
- `/discord:access` status and `group add` both warn about duplicate groups across local/global (informational, non-blocking)
- `group add --local` skips the interactive prompt and writes to the project-local file, or errors if DISCORD_PROJECT_DIR is unset
- `group add --global` skips the interactive prompt and writes to the global file
- `patches/discord-local-scoping.patch` encodes all changes (server.ts + .mcp.json + updated SKILL.md) in one file, verified by dry-run
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-01-SUMMARY.md` following the summary template.
</output>
