---
phase: 02-skill-side-scope-awareness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03, SKIL-04, SKIL-05]

must_haves:
  truths:
    - "/discord:access (no args) shows a 'Using: local ...' or 'Using: global ...' banner before the status body"
    - "Running /discord:access group add <id> when DISCORD_PROJECT_DIR is set prompts local vs global choice"
    - "Choosing 'local' causes the skill to write to <projectDir>/.claude/channels/discord/access.json"
    - "Choosing 'global' causes the skill to write to ~/.claude/channels/discord/access.json (unchanged behavior)"
    - "Running /discord:access group add <id> when DISCORD_PROJECT_DIR is unset writes to global with no prompt"
    - "/discord:access group rm <id> removes the group from whichever file contains it (local or global)"
    - "pair, deny, allow, remove, policy always read/write the global access.json"
    - "set reads/writes the active (resolved) file"
  artifacts:
    - path: "~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md"
      provides: "Full updated skill definition with scope-aware logic"
      contains: "DISCORD_PROJECT_DIR"
    - path: "~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md"
      contains: "resolveScope"
  key_links:
    - from: "SKILL.md dispatch block (no args)"
      to: "resolveScope helper"
      via: "called at top of every operation to determine active path"
    - from: "group add scope prompt"
      to: "AskUserQuestion"
      via: "D-01: AskUserQuestion for local/global choice"
    - from: "group rm"
      to: "both local and global paths"
      via: "reads both files, removes from whichever contains the group"
---

<objective>
Rewrite SKILL.md with full scope-awareness: a resolveScope helper that mirrors server.ts resolveAccessFile(), a status banner (SKIL-01), group add scope prompt (SKIL-02/03/04), and group rm dual-file search (SKIL-05). pair/deny/allow/remove/policy stay global per D-12. set follows active file per D-13.

Purpose: The skill must independently know which file to read/write — it cannot rely on the server having already resolved this.
Output: Updated SKILL.md in the discord plugin cache.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/haocheng_mini/Documents/projects/claude_code_channels/.planning/ROADMAP.md
@/Users/haocheng_mini/Documents/projects/claude_code_channels/.planning/REQUIREMENTS.md
@/Users/haocheng_mini/Documents/projects/claude_code_channels/.planning/phases/02-skill-side-scope-awareness/02-CONTEXT.md

<!-- Key decisions constraining this plan: -->
<!-- D-01: AskUserQuestion for scope choice -->
<!-- D-02: Default to "local" when DISCORD_PROJECT_DIR is set -->
<!-- D-03: If DISCORD_PROJECT_DIR unset: warn + write to global without prompting -->
<!-- D-04: Discover project dir via DISCORD_PROJECT_DIR env var (not pwd) -->
<!-- D-05: When local is active, show only local file contents -->
<!-- D-06: Banner: "Using: local (.claude/channels/discord/access.json)" or "Using: global (~/.claude/channels/discord/access.json)" -->
<!-- D-07: No local config → show global with global banner -->
<!-- D-08: group rm searches both local and global — removes from whichever has it -->
<!-- D-09: If group not found in either, hint which file to check -->
<!-- D-10/D-11: SKILL.md changes go into discord-local-scoping.patch (handled in Plan 02) -->
<!-- D-12: pair/deny/allow/remove/policy → always global -->
<!-- D-13: set → active file (local or global) -->
<!-- DEFERRED (Phase 3): --local/--global flags (SKIL-06, SKIL-07) -->
</context>

<interfaces>
<!-- From server.ts (Phase 1 — established patterns for the skill to mirror) -->

<!-- resolveAccessFile() logic (mirror this in SKILL.md as resolveScope): -->
<!--   1. Read DISCORD_PROJECT_DIR env var via Bash -->
<!--   2. If set: check if <projectDir>/.claude/channels/discord/access.json exists -->
<!--   3. If exists: use local path, scope = "local" -->
<!--   4. If not exists: use global path, scope = "global" -->
<!--   5. If DISCORD_PROJECT_DIR unset: use global path, warn user -->

<!-- Global path: ~/.claude/channels/discord/access.json -->
<!-- Local path: <DISCORD_PROJECT_DIR>/.claude/channels/discord/access.json -->

<!-- Skill allowed tools: Read, Write, Bash(ls *), Bash(mkdir *) -->
<!-- Skill CANNOT use: Bash(cat ...), Bash(echo ...), Bash(patch ...) -->
<!-- Skill CAN use Bash to check env: Bash(printenv DISCORD_PROJECT_DIR) or equivalent -->
<!-- Note: Bash(ls *) is the only bash form — use ls to check file existence -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write updated SKILL.md with scope-aware logic</name>
  <files>~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md</files>
  <read_first>
    - ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md (current content — understand every operation before changing)
    - ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/server.ts (lines 1-70 — resolveAccessFile() pattern to mirror)
    - /Users/haocheng_mini/Documents/projects/claude_code_channels/.planning/phases/02-skill-side-scope-awareness/02-CONTEXT.md (all decisions D-01 through D-13)
  </read_first>
  <action>
    Rewrite SKILL.md with these precise changes. Keep all existing frontmatter and sections intact; only modify/add the described pieces.

    **1. Add a "Scope resolution" section near the top (after State shape, before Dispatch):**

    ```markdown
    ## Scope resolution

    Before any read or write, resolve which access.json to use:

    1. Run `Bash(ls ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/server.ts)` is NOT how to get the env var. Instead, use `Bash(printenv DISCORD_PROJECT_DIR)` — if output is non-empty, PROJECT_DIR is set.
    2. If PROJECT_DIR is set: check if `<PROJECT_DIR>/.claude/channels/discord/access.json` exists by running `Bash(ls <PROJECT_DIR>/.claude/channels/discord/access.json 2>/dev/null)`. If exit is 0 (file listed): SCOPE=local, ACTIVE_PATH=<PROJECT_DIR>/.claude/channels/discord/access.json.
    3. If PROJECT_DIR not set, or local file absent: SCOPE=global, ACTIVE_PATH=~/.claude/channels/discord/access.json.

    Call this at the start of every operation that reads or writes access.json. Exceptions: pair, deny, allow, remove, policy always use ACTIVE_PATH=~/.claude/channels/discord/access.json (global only). set uses ACTIVE_PATH (active file).
    ```

    **2. Update "No args — status" dispatch block:**

    Replace the current status steps with:

    ```markdown
    ### No args — status

    1. Run scope resolution (above) to get ACTIVE_PATH and SCOPE.
    2. Print banner: `Using: local (.claude/channels/discord/access.json)` when SCOPE=local, or `Using: global (~/.claude/channels/discord/access.json)` when SCOPE=global.
    3. Read ACTIVE_PATH (handle missing file with default).
    4. Show: dmPolicy, allowFrom count and list, pending count with codes + sender IDs + age, groups count.
    ```

    **3. Update "group add" dispatch block:**

    Replace the current group add steps with:

    ```markdown
    ### `group add <channelId>` (optional: `--no-mention`, `--allow id1,id2`)

    1. Run scope resolution to get PROJECT_DIR and whether local file exists.
    2. If PROJECT_DIR is set:
       a. Ask user: "Add channel <channelId> to your project-local config (.claude/channels/discord/access.json) or global config (~/.claude/channels/discord/access.json)? [local/global]" — use AskUserQuestion. Default suggestion: "local" (first option, consistent with D-02).
       b. If user answers "local": TARGET_PATH=<PROJECT_DIR>/.claude/channels/discord/access.json, create dir if needed with `Bash(mkdir -p <PROJECT_DIR>/.claude/channels/discord)`.
       c. If user answers "global": TARGET_PATH=~/.claude/channels/discord/access.json.
    3. If PROJECT_DIR is not set: warn "Note: DISCORD_PROJECT_DIR is not set — project-local scoping unavailable. Writing to global config." TARGET_PATH=~/.claude/channels/discord/access.json.
    4. Read TARGET_PATH (create default if missing).
    5. Set `groups[<channelId>] = { requireMention: !hasFlag("--no-mention"), allowFrom: parsedAllowList }`.
    6. Write to TARGET_PATH (pretty-print, 2-space indent).
    ```

    **4. Update "group rm" dispatch block:**

    Replace the current group rm steps with:

    ```markdown
    ### `group rm <channelId>`

    1. Run scope resolution to get PROJECT_DIR. Determine both candidate paths:
       - LOCAL_PATH = <PROJECT_DIR>/.claude/channels/discord/access.json (if PROJECT_DIR set)
       - GLOBAL_PATH = ~/.claude/channels/discord/access.json
    2. Check if the group exists in local config (if PROJECT_DIR set): Read LOCAL_PATH, check if `groups[<channelId>]` is present.
    3. Check if the group exists in global config: Read GLOBAL_PATH, check if `groups[<channelId>]` is present.
    4. If found in local: delete `groups[<channelId>]`, write LOCAL_PATH, confirm "Removed from local config."
    5. If found in global (and not already removed): delete `groups[<channelId>]`, write GLOBAL_PATH, confirm "Removed from global config."
    6. If found in neither: say "Channel <channelId> not found in local config or global config."
    7. If found in both (unlikely but possible): remove from both, confirm "Removed from both local and global config."
    ```

    **5. Update "pair", "deny", "allow", "remove", "policy" sections:**

    Add a note at the top of each of these sections: "Note: this operation always uses the global access.json (`~/.claude/channels/discord/access.json`) regardless of project scope." Hardcode the global path (`~/.claude/channels/discord/access.json`) in the Read/Write steps for these operations — do not use ACTIVE_PATH.

    **6. Update "set" section:**

    Add to the beginning: "Run scope resolution to get ACTIVE_PATH." Then change "Read, set the key, write" to "Read ACTIVE_PATH, set the key, write to ACTIVE_PATH."

    Keep all other sections (frontmatter, State shape, Implementation notes) unchanged.
  </action>
  <verify>
    <automated>grep -n "DISCORD_PROJECT_DIR\|resolveScope\|Scope resolution\|Using: local\|Using: global\|AskUserQuestion\|TARGET_PATH\|ACTIVE_PATH" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Scope resolution" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 1 match
    - `grep "DISCORD_PROJECT_DIR" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 3 matches (referenced in scope resolution, group add, group rm)
    - `grep "Using: local" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns exactly 1 match (in status section)
    - `grep "Using: global" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns exactly 1 match (in status section)
    - `grep "AskUserQuestion" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns exactly 1 match (in group add section)
    - `grep "both" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 1 match in the group rm section
    - `grep "always uses the global" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 4 matches (pair, deny, allow, remove, policy sections)
    - The frontmatter `allowed-tools` block still includes `Bash(ls *)` and `Bash(mkdir *)`
    - `grep "printenv DISCORD_PROJECT_DIR" ~/.claude/plugins/cache/claude-plugins-official/discord/0.0.1/skills/access/SKILL.md` returns at least 1 match
  </acceptance_criteria>
  <done>SKILL.md contains scope resolution helper, status banner, group add scope prompt with AskUserQuestion, group rm dual-file search, pair/deny/allow/remove/policy hardcoded to global, set using active path.</done>
</task>

</tasks>

<verification>
Read the updated SKILL.md and verify the full dispatch table covers all operations with correct file routing:
- No args: resolves scope, shows banner, reads ACTIVE_PATH
- group add: prompts when PROJECT_DIR set, writes to user-chosen path
- group rm: searches both files, removes from whichever has it
- pair/deny/allow/remove/policy: hardcoded global path
- set: uses ACTIVE_PATH
</verification>

<success_criteria>
1. SKILL.md contains "Scope resolution" section with DISCORD_PROJECT_DIR lookup via printenv
2. Status dispatch shows "Using: local ..." or "Using: global ..." banner (per D-06)
3. group add uses AskUserQuestion with "local" as default option (per D-01, D-02)
4. group add with PROJECT_DIR unset warns and writes to global without prompting (per D-03)
5. group rm reads both files and removes from whichever contains the group (per D-08)
6. pair/deny/allow/remove/policy explicitly use global path (per D-12)
7. set uses ACTIVE_PATH (per D-13)
</success_criteria>

<output>
After completion, create `/Users/haocheng_mini/Documents/projects/claude_code_channels/.planning/phases/02-skill-side-scope-awareness/02-01-SUMMARY.md`
</output>
